name: pr-success
on:
  pull_request: {}

jobs:
  pr-success:
    name: Test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v1
      - name: Install Deps
        run: npm ci
      - name: Test
        run: npm test
  eslint:
    name: Eslint
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v1
      - name: Lint
        run: npm run lint
